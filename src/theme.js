import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		mode: 'dark',
		primary: {
			main: "#2176FF",
			secondary: "#33A1FD",
		},
	},

});

export default theme;
